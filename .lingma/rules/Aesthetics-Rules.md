---
trigger: always_on
---
# Aesthetics & Theming
- **背景图片支持**:
  - 允许配置图片路径。
  - 自动处理终端透明度（通过发送 ANSI 转义序列或配合支持透明度的终端宿主）。
- **主题系统**:
  - 预设 `Dark`, `Light`, `Dracula`, `Nord` 等经典主题。
  - 用户自定义主题仅需修改一个颜色变量对象。
- **区分度设计**：不同区域的按钮支持自定义背景色，用户可以通过颜色（而非文字）快速定位功能（如：绿色是开发环境，红色是生产环境）。