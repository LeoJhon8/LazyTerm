# 按钮功能验证测试

## App状态
- ✅ JavaScript语法检查通过 (`node -c terminal.js`)
- ✅ Electron应用已运行 (10个进程)
- ✅ 修复了以下问题:
  - 删除了对已移除DOM元素的引用 (`terminalContent`, `commandInput`, `prompt`)
  - 修复了重复代码导致的语法错误
  - 修复了try/catch块不匹配的问题

---

## 按钮功能列表与测试步骤

### 1. 新建标签页按钮 (+)
- **位置**: 标签页栏右侧
- **点击预期**: 创建新标签页，启动新的PTY会话
- **测试步骤**:
  1. 点击 `+` 按钮
  2. 应该看到新标签页出现
  3. 新标签页应显示终端提示符

### 2. 新建连接按钮 (📡)
- **位置**: 顶部工具栏
- **点击预期**: 打开连接对话框
- **测试步骤**:
  1. 点击 📡 按钮
  2. 应该弹出"New Connection"对话框
  3. 可以选择 Local / SSH / Telnet

### 3. 保存会话按钮 (💾)
- **位置**: 顶部工具栏
- **点击预期**: 打开连接对话框，可保存为会话
- **测试步骤**:
  1. 点击 💾 按钮
  2. 应该弹出"New Connection"对话框
  3. 会话名称框应该可以填写

### 4. 快捷命令按钮 (⚡)
- **位置**: 顶部工具栏
- **点击预期**: 切换快捷命令栏显示/隐藏
- **测试步骤**:
  1. 点击 ⚡ 按钮
  2. 快捷命令栏应该在下方显示/隐藏切换

### 5. 历史记录按钮 (📜)
- **位置**: 顶部工具栏
- **点击预期**: 切换历史记录侧边栏显示/隐藏
- **测试步骤**:
  1. 点击 📜 按钮
  2. 历史记录侧边栏应该在右侧显示/隐藏切换

### 6. 快捷命令栏按钮
- **位置**: 底部快捷命令栏
- **按钮列表**: help, clear, history, dir, pwd, echo
- **点击预期**: 执行对应命令到终端
- **测试步骤**:
  1. 点击 `help` 按钮 → 应该执行 `help` 命令
  2. 点击 `clear` 按钮 → 应该清空终端
  3. 点击 `dir` 按钮 → 应该列出目录

### 7. 添加快捷命令按钮 (+)
- **位置**: 快捷命令栏右侧
- **点击预期**: 打开添加快捷命令对话框
- **测试步骤**:
  1. 点击 `+` 按钮
  2. 应该弹出"Add New Shortcut"对话框

### 8. 会话侧边栏添加按钮 (+)
- **位置**: 会话侧边栏标题栏
- **点击预期**: 打开连接对话框创建新会话
- **测试步骤**:
  1. 点击会话侧边栏的 `+` 按钮
  2. 应该弹出"New Connection"对话框

### 9. 清除历史按钮 (🗑️)
- **位置**: 历史记录侧边栏标题栏
- **点击预期**: 清除所有命令历史
- **测试步骤**:
  1. 点击 🗑️ 按钮
  2. 应该弹出确认对话框
  3. 确认后历史列表清空

---

## 标签页操作

### 关闭标签页 (×)
- **位置**: 每个标签页右侧
- **点击预期**: 关闭标签页和对应的PTY会话
- **测试步骤**:
  1. 创建2个标签页
  2. 点击其中一个的 `×` 按钮
  3. 该标签页应该被关闭

### 重命名标签页
- **操作**: 双击标签页标题
- **点击预期**: 可以重命名标签页
- **测试步骤**:
  1. 双击标签页标题
  2. 输入新名称
  3. 标签标题更新

---

## 快捷键

### Ctrl+Shift+N
- **操作**: 按下组合键
- **预期**: 打开新建连接对话框

### Ctrl+S
- **操作**: 按下组合键
- **预期**: 弹出输入框，可保存当前会话

---

## 滚轮缩放
- **操作**: 在终端区域按住Ctrl + 向上/向下滚动
- **预期**: 增大/减小字体大小

---

## 浸模式检查清单

启动应用后，依次测试以下项目：

- [ ] 点击 `+` 按钮创建新标签页
- [ ] 点击 📡 按钮打开连接对话框
- [ ] 点击 💾 按钮打开保存会话对话框
- [ ] 点击 ⚡ 按钮切换快捷命令栏
- [ ] 点击 📜 按钮切换历史记录侧边栏
- [ ] 点击快捷命令按钮 (help, clear, dir等)
- [ ] 点击快捷命令栏 `+` 按钮添加新快捷命令
- [ ] 关闭标签页
- [ ] 双击标签页重命名
- [ ] 按 Ctrl+Shift+N 打开连接对话框
- [ ] 按 Ctrl+S 保存会话
- [ ] 滚轮缩放字体大小

---

## 如果按钮仍失效

1. **打开DevTools**: 按 `Ctrl+Shift+I`
2. **查看Console标签**: 查找红色错误信息
3. **截图或复制错误**: 并报告问题

常见的错误可能包括:
- `Cannot read property of null` - DOM元素未找到
- `SyntaxError` - 代码语法错误 (已修复)
- `ReferenceError` - 变量或方法未定义

---

## 已知限制

1. TabManager类中的历史记录功能已修复，现在可以正常发送命令到PTY
2. 快捷命令的multi-line命令功能已修复，可以正确发送多行命令
3. 会话保存功能正常 (保存连接参数，不保存终端内容)

---

## 下一步

如果所有按钮都正常工作，可以进行以下进一步测试:
1. SSH连接测试
2. 交互式程序测试 (vim, top)
3. 会话加载测试

如有问题，请查看Console日志中的错误信息。
