# LazyTerm 项目规则

## 验证约定

- 不再尝试做浏览器截图验证。前端改动以代码审查、类型检查、编译检查结果或用户明确要求的其他验证方式为准。
- 禁止 AI 使用 `build` 类命令作为默认验证方式，包括但不限于 `npm run build`、`npm run tauri:build`、`cargo build`。
- 默认验证只允许使用编译类检查，例如 `tsc --noEmit`、`cargo check`；除非用户明确要求，不使用 `cargo check --tests`。
- 禁止 AI 创建、修改、保留或运行任何测试代码，包括单元测试、集成测试、端到端测试、临时测试、测试 mock、测试 fixture、测试脚本和 `__tests__` / `tests` 目录内容。
- 禁止 AI 在业务代码中编写任何测试或验证辅助代码，包括临时测试分支、验证用 mock、仅用于测试的日志或开关。
- 修改后的行为由用户进行快速人工验证；如果用户发现 bug，AI 再根据反馈快速修复。
- 临时验证只能通过代码审查、编译类命令或用户明确要求的一次性手工检查完成，不得污染业务代码。
- 当前编译检查失败时，AI 必须说明失败原因、影响范围，以及失败是否与本次修改相关。
