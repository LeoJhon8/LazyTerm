{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],  
  "compilerOptions": {
    // ... 其他配置
    "baseUrl": ".", // 或者 "src"，取决于你的项目结构
    "paths": {
      "@/*": ["src/*"], // 如果 @ 指向 src 目录
      // 或者如果 @ 指向根目录下的 types 文件夹：
      // "@/types/*": ["types/*"] 
    }
  },
}
